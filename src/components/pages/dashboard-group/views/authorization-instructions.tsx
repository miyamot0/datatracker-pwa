export default function AuthorizationInstructions() {
  return (
    <>
      <p>
        Web-apps need to have explicit authorization from users to access local
        folders. You must clearly and consistently endorse that you approve that
        the app do this each time it is loaded.
      </p>
      <p>
        You can authorize the application to use the respective folder using the
        button featured in the upper right area of the screen.
      </p>
    </>
  );
}
